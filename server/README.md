# Chat App Server

Server is a simple Express + Socket.io demo. Run:

```powershell
cd server
npm install
npm run dev
```

This server implements:
- username auth (simple)
- join/leave rooms
- global room messaging
- private messages
- typing indicators
- read receipts
- simple in-memory message store (for demo)

It's for demo/development only; not production-ready.